import { createTheme } from '@mui/material';

const theme = createTheme({
  palette: {
    primary: {
      main: '#00bcd4'
    },
    third: {
      main: 'black'
    },
    white: {
      main: 'white'
    }
  }
});

export default theme;
